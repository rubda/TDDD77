language: c
script: 
  - echo "Running unit tests"
  - make test
  - echo "Checking for memory leaks"
  - bash -c "cd quadopt/obj/tests && ./test_solver valgrind --leak-check=full --error-exitcode=1"
notifications:
  email:
    on_success: never
    on_failure: always
